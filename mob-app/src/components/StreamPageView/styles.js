import {StyleSheet} from "react-native";

export default styles = StyleSheet.create({
    videosContainer: {
        flex: 1,
        flexDirection: 'row',
        justifyContent: 'center',
        position: 'relative',
    },
});
